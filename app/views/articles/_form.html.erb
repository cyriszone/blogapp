
<% # form_for @article, url: articles_path, method: :post do |f| %>
<%= form_for @article, url: articles_search_path, method: :get do |f| %>

<% # url: articles_path, what??? %>
<% # what is the default URL??? Can Rails tell? %>

  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibitedthis article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <p>
    <%= f.text_area :text %> <%= f.submit "search!"%>
  </p>

  <p>

    <% # if @article is new, submit to generate a Create Article Link. Otherwise Update Article %>
  </p>
<% end %>
